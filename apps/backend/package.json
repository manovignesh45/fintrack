{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "GOPROXY=direct go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "migrate": "migrate -path ./internal/db/migrations -database \"$DATABASE_URL\""
  }
}
